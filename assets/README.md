
ViewFilter: Adds a simple filter input for any views (widgets/components). 

- ReArray.js -- Helper class for testing match of an array of strings.
- ViewFilter.js -- Adds a simple filter input for any views (widgets).